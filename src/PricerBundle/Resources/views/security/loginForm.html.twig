{% extends '@Pricer/main_layout.html.twig' %}

{% block content %}

    {#{% if error %}#}
    {#<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>#}
    {#{% endif %}#}
    {#<form action="{{ path('pricer_user_login') }}" method="post">#}
    {#<label for="email">Email</label>#}
    {#<input type="email" id="email" name="_username" value="{{ last_username }}">#}

    {#<label for="password">Пароль</label>#}
    {#<input type="password" id="password" name="_password">#}

    {#<button type="submit">Войти</button>#}
    {#</form>#}

    {{ form_start(form) }}
    {% if error %}
        {#<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>#}
        <div class="label label-danger">Вы ввели неверные данные!</div>
    {% endif %}
    <div class="form-group">
        {{ form_label(form._username) }}
        {{ form_widget(form._username, {'attr':{ 'class': 'form-control'}} ) }}
    </div>
    <div class="form-group">
        {{ form_label(form._password) }}
        {{ form_widget(form._password, {'attr':{ 'class': 'form-control'}} ) }}
    </div>
    <div class="form-group">
        {{ form_widget(form.submit, {'attr':{ 'class': 'btn btn-success'}} ) }}
    </div>
    {{ form_end(form) }}

{% endblock %}