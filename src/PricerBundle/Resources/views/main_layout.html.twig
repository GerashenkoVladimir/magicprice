<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Magic price</title>

    <!-- Bootstrap -->
    <link rel="stylesheet" href="{{ asset('bundles/pricer/bootstrap-3.3.6-dist/css/bootstrap.css') }}">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
    {% block stylesheet %}

    {% endblock %}
</head>
<body>
<div class="navbar navbar-inverse navbar-static-top" id="main_menu">
    <div class="container">
        <div class="navbar-header">
            <a href="{{ path('pricer_homepage') }}" class="navbar-brand">Magic price</a>
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-menu">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse" id="main-menu">
            <ul class="nav navbar-nav">
                <li><a href="{{ path('pricer_homepage') }}">Главная</a></li>
                <li><a href="#{#{{ path('price_showAllSuppliers') }}#}">Поставщики</a></li>
                <li><a href="#{#{{ path('price_showAllProducers') }}#}">Производители</a></li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle main-menu-links" data-toggle="dropdown">Ссылка<b
                                class="caret"></b></a>
                    <ul class="dropdown-menu">
                        {#{% for  category in categories %}#}
                        {#<li><a href="#">{{ category.name }}</a></li>#}
                        {#{% endfor %}#}
                    </ul>
                </li>
                <li><a href="{{ path('pricer_user_login') }}">Авторизация</a></li>
                <li><a href="{{ path('pricer_user_registration') }}">Регистрация</a></li>


            </ul>
        </div>
    </div>
</div>
<div class="container">
    {% block content %}
        <div class="row">
            <div class="col-xs-2">
                {% block leftSideBar %}

                {% endblock %}
            </div>
            <div class="col-xs-10">
                {% block actionPanel %}

                {% endblock %}
            </div>
        </div>
    {% endblock %}
</div>

<!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
<script src="{{ asset('bundles/pricer/js/jQuery1.11.3.js') }}"></script>
<!-- Include all compiled plugins (below), or include individual files as needed -->
<script src="{{ asset('bundles/pricer/bootstrap-3.3.6-dist/js/bootstrap.js') }}"></script>
<script src="{{ asset('bundles/pricer/helpers/mainHelper.js') }}"></script>
{% block javascript %}

{% endblock %}
</body>
</html>