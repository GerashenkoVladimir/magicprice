/*
$(function () {
    $('#supplier_add_button').bind('click', function (event) {
        var target = $(event.target);
        runSimpleAjax(target.data("url"),{}, function (html) {
            if(html){
                $("#action_panel").html(html);
            }
        }, "GET");
    });
});*/

$(function () {
    
});
